<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= product.name %></title>
  </head>
  <body>
    <h1><%= product.name %></h1>
    <p>
      カテゴリ：<a href="/products?category=<%= product.category %>"
        ><%= product.category %></a
      >
    </p>
    <p><%= product.price %>円/1個</p>
    <div>
      <a href="/products">一覧に戻る</a>
    </div>
    <div>
      <a href="/products/<%= product._id %>/edit">編集する</a>
    </div>
    <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
      <button>削除する</button>
    </form>
  </body>
</html>
